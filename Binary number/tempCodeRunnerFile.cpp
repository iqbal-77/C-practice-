cout <<  "sum = " << sumNum(n) << endl;
    cout << "Binary to decimal: " << binToDec(n) << endl;
    cout << "Decimal to Binary: " << binNum(n) << endl;
    for(int i = 2; i <= n; i++){

    cout << binNum(n) << endl;
    }
    int binary = binNum(n);
    cout << binary << endl;